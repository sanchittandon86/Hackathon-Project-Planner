/**
 * Server component for dashboard navigation cards
 * 
 * This component previously rendered feature navigation cards.
 * Navigation is now handled via clickable stat cards in DashboardStats.
 */


export default function DashboardCards() {
  // All navigation cards have been removed - navigation is now via stat cards
  return null;
}
